import { Search } from "../Search/Search"
import {Pagination} from "../Pagination/Pagination";

export const Packs = () => {
    return (
        <div>
            <Search />
            <Pagination />
        </div>
    )
}